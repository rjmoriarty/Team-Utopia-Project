using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pickupable : MonoBehaviour {
	// This script just exists so it can be assigned as a detectable component of "Pickupable" objects.
	// I think it's bonkers but I don't know a better way.
	// See PlayerInteract.cs
	// Adapted from Unity3D Tutorial - Pickup and Carry Objects by unitylessons.com at https://www.youtube.com/watch?v=runW-mf1UH0

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
